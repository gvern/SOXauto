# Core dependencies for data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0  # For parquet file support
openpyxl>=3.1.2  # For reading Excel files

# Database connectivity
pyodbc>=4.0.39  # Keep for backward compatibility (legacy)

# AWS dependencies (Secrets Manager retained; Athena tooling removed)
boto3>=1.28.0  # AWS SDK for Python
botocore>=1.31.0

# Serverless/HTTP stack removed (Temporal Worker-based orchestration)
# Flask
# gunicorn
# awslambdaric

# Utilities and logging
python-dateutil>=2.8.0
pytz>=2023.3
python-json-logger>=2.0.0  # Structured JSON logging

# Development and testing (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.7.0
flake8>=6.0.0

# Security and performance
cryptography>=41.0.0
requests>=2.31.0

# Workflow orchestration
temporalio>=1.5.0  # Temporal.io Python SDK for workflow orchestration

# Frontend UI
streamlit>=1.28.0  # Web UI framework for the demo application